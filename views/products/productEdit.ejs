<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "Editar producto"}); %>
<body>
    <%- include('../partials/header'); %>
    <main class="newProduct">
        <div class="back-arrow">
            <a href="/">
                <button class="arrow">
                    <i class="fas fa-chevron-left"></i>
                </button>
            </a>
        </div>
        <h2 class="session-title">EDITAR UN PRODUCTO</h2>
        <form class="session-form newProductForm" action="/products/edit/<%= product.id%>?_method=PUT" method="POST" enctype="multipart/form-data">
            <div class="input-form">
                <label for="name">NOMBRE</label>
                <input type="text" name="name" id="productName" class="inputs" placeholder="Escriba el nombre del producto" value="<%= product.name %> ">
            </div>
            <div class="input-form">
                <label for="description">DESCRIPCIÓN</label>
                <textarea name="description" id="productDescription" class="inputs" placeholder="Realice una descripción del producto"><%= product.description %> </textarea>
            </div>
            <div class="two-cols">
                <div class="col">
                    <label for="category">CATEGORÍA</label>
                    <select name="category" class="btn-talle">
                        <option value="" hidden>Seleccione</option>
                        <option value="t-shirts"<%= product.category == "t-shirts"? "selected":"" %> >Remera</option> 
                        <option value="shirts" <%= product.category == "shirts"? "selected":"" %>>Camisa</option> 
                        <option value="jackets"<%= product.category == "jackets"? "selected":"" %>>Campera</option>
                        <option value="pants"<%= product.category == "pants"? "selected":"" %>>Pantalón</option>
                        <option value="accesories"<%= product.category == "accesories"? "selected":"" %>>Accesorio</option>
                    </select>    
                </div>
                
                <div class="col">
                    <label for="size">TALLE</label>
                    <select name="size" class="btn-talle">
                            <option value="" hidden>Talle</option>
                            <option value="s"<%= product.size == "s"? "selected":"" %>>S</option> 
                            <option value="m"<%= product.size == "m"? "selected":"" %>>M</option>
                            <option value="l"<%= product.size == "l"? "selected":"" %>>L</option>
                            <option value="xl"<%= product.size == "xl"? "selected":"" %>>XL</option>
                            <option value="xxl"<%= product.size == "xxl"? "selected":"" %>>XXL</option>
                    </select>    
                </div>
                
            </div>

            <div class="two-cols">
                <div class="col">
                    <label for="price">PRECIO</label>
                    <input type="number" name="price" id="productPrice" class="inputs" placeholder="Ingrese el precio del producto" value="<%= product.price%>" min="0">        
                </div>
                <div class="col">
                    <label for="stock">STOCK</label>
                    <input type="number" name="stock" id="productStock" class="inputs" placeholder="Ingrese el stock del producto" value="<%= product.stock%>" min="0">    
                </div>
            </div>

            <div class="two-cols" style="margin-bottom: 0;">
                <div>IMAGEN ACTUAL</div>
                <div>IMAGEN NUEVA</div>
            </div>
            <div class="two-cols" style="margin-top: 0;">
                <div class="col" style="display: flex; align-items: center; border: 1px solid black;">
                    <div style="width: 50%">
                        <img src="/images/products/<%= product.image%>" alt="<%= product.alt %>" style="width: 100%;">
                    </div>      
                </div>
                <div class="col" style="justify-content: center; border: 1px solid black;">
                    <div class="input-form" style="margin: 0; padding: 0;">
                        <!-- <label for="productIMG">IMAGEN</label> -->
                        <input type="file" name="image" id="productIMG" class="inputs">
                    </div>  
                </div>
            </div>

            <b style="margin: 0 auto 1rem; width: 90%;">*Si no selecciona una imagen nueva, se conservará la imagen actual.</b>

            <div class="btn">
                <button type="submit" class="btn-black">EDITAR PRODUCTO</button>
            </div>
        </form>
    </main>
    <%- include('../partials/footer'); %>
</body>
</html>